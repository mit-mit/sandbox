An absolute bare-bones web app using Dart vNext static interop and Dart to WasmGC compilation.

## Building

Run `build.sh <path to dart cli>`

## Running

Run a web server on the `build` directory.

